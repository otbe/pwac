{
  "private": true,
  "scripts": {
    "release": "workspace-version",
    "build": "yarn wsrun build --stages --exclude example-react --exclude example-elm",
    "lint": "yarn wsrun lint --fast-exit",
    "test": "yarn wsrun test --serial --exclude-missing --fast-exit"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "workspace-release": "^0.4.4",
    "wsrun": "^2.2.1"
  }
}
