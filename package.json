{
  "private": true,
  "scripts": {
    "release": "workspace-version",
    "build": "yarn wsrun --stages --exclude example-react build",
    "lint": "yarn wsrun --fast-exit lint",
    "test": "yarn wsrun --serial --exclude-missing --fast-exit test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "workspace-release": "^0.4.5",
    "wsrun": "^3.4.0"
  }
}
