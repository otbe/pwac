{
  "private": true,
  "scripts": {
    "release": "workspace-version",
    "build": "yarn wsrun build --stages --exclude example-react --exclude example-elm",
    "lint": "yarn wsrun lint",
    "test": "yarn wsrun test --serial --exclude-missing"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "workspace-release": "^0.4.3",
    "wsrun": "^2.2.1"
  }
}
